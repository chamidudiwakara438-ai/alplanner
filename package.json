{
  "name": "al-planner",
  "version": "1.1.0",
  "private": true,
  "description": "AL Planner — Netlify static site + Functions (student accounts in Blobs). No Python.",
  "main": "scripts/preview.js",
  "scripts": {
    "start": "node scripts/preview.js",
    "preview": "node scripts/preview.js"
  },
  "engines": {
    "node": ">=18"
  }
}
